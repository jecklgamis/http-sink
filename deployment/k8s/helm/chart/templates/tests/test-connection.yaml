apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "http-sink.fullname" . }}-test-connection"
  labels:
    {{- include "http-sink.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "http-sink.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
